{
  "name": "plodo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Plodo — carnet de visites techniques piloté par la voix. Local-first, zéro token API.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server/index.mjs",
    "dev": "node --watch server/index.mjs",
    "reindex": "node server/rag/build.mjs",
    "ask": "node server/rag/cli.mjs"
  }
}
